---
"@sapiom/harness": patch
---

Keep the rail's profile menu readable when the rail is dragged narrow: the panel now grows to its widest row instead of inheriting the rail's width and wrapping labels on top of each other.
