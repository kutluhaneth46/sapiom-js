---
"@sapiom/harness": patch
---

Keep the rail's profile menu readable when the rail is dragged narrow: the panel now carries its own readable width (the studio rail's `--menu-w`) instead of inheriting the rail's width and wrapping labels on top of each other.
